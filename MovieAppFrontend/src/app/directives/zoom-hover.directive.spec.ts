import { ElementRef, Renderer2 } from '@angular/core';
import { ZoomHoverDirective } from './zoom-hover.directive';

describe('ZoomHoverDirective', () => {
  it('should create an instance', () => {
    // const directive = new ZoomHoverDirective(private elementRef:ElementRef,private renderer2:Renderer2);
    // const directive = new ZoomHoverDirective();
    // expect(directive).toBeTruthy();
  });
});
